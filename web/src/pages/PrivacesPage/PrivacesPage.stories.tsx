import type { Meta, StoryObj } from '@storybook/react'

import PrivacesPage from './PrivacesPage'

const meta: Meta<typeof PrivacesPage> = {
  component: PrivacesPage,
}

export default meta

type Story = StoryObj<typeof PrivacesPage>

export const Primary: Story = {}
